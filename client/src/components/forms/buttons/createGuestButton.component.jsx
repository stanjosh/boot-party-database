import React from 'react';

const CreateGuestButton = ({ customerData }) => {
    return (
        <button onClick={handleClick}>Create Guest</button>
    );

    function handleClick() {
        // Add your logic here
    }
};

export default CreateGuestButton;
